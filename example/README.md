# Sara Example

Run the example project :
```sh
gleam run
```

Regenerate the JSON boilerplate :
```sh
gleam run -m build_runner
```

Watch for file change and auto regenerate JSON boilerplate :
```sh
gleam run -m build_runner watch
```
